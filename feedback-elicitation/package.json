{
  "name": "@ac-reusable-dialogs/feedback-elicitation",
  "version": "0.0.3",
  "publisher": "Amazon",
  "description": "ACDL reusable dialog for eliciting feedback from users",
  "license": "AmazonSoftwareLicense",
  "scripts": {
    "clean": "rm -rf build && rm -rf *.tgz",
    "build": "acc",
    "release": "npm run clean && npm run build && npm pack"
  },
  "dependencies": {},
  "devDependencies": {
    "@alexa/acdl": "^0.1.0"
  },
  "ask": {
    "srcDir": "src"
  }
}
